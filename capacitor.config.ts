import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'OneForAll TV',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
